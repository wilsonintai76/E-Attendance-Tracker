{
  "name": "e-attendance-server",
  "private": true,
  "version": "0.3.8",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "node ../scripts/deploy.mjs",
    "db:init": "wrangler d1 execute e-attendance-db --file=./src/db/schema.sql",
    "db:local": "wrangler d1 execute e-attendance-db --local --file=./src/db/schema.sql",
    "types": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "wrangler": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
